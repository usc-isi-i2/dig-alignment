PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
select ?ad ?feature ?name ?phone where {
  ?ad <http://dig.isi.edu/ontology/personFeature>?feature .
  optional { ?ad rdfs:label ?name . }
  optional { ?ad <http://dig.isi.edu/ontology/tenDigitPhoneNumber> ?phone . }
}
order by ?ad